# GitHub 自动生成视频说明

这个项目已经配置了 GitHub Actions。

上传到 GitHub 后，只要运行 `build-hometown-video` 工作流，就会自动生成一个竖屏 MP4 视频。

## 会做什么

1. 读取老家清晨田边图片。
2. 读取口播字幕文件。
3. 在 GitHub 服务器安装 `ffmpeg` 和中文字体。
4. 尝试生成中文口播音频。
5. 把图片、字幕、音频合成一个竖屏 MP4。
6. 把最终视频作为 GitHub Actions 产物保存。

## 最终视频位置

GitHub Actions 跑完后，在工作流页面下载：

`hometown-final-video`

里面的文件是：

`2026-07-09_老家水田_最终成片.mp4`

## 如果口播音频失败

脚本会自动改成静音视频，但字幕仍然会正常显示。

后面可以在剪映里用自己的声音重新录一遍。

